#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;

    vector<int> numbers(5, n);
    for (int element : numbers)
    {
        cout << element << " ";
    }

    return 0;
}